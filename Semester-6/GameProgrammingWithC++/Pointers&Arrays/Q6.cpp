#include <iostream>
using namespace std;

void swap(int B[]) {
    int x=B[0];
    for(int k=0;k<5;k++)
    {
    	B[i]=B[i+1];
    }
    B[5]=x;
}

int main() {
    int A[5];
    for(int i=0;i<6;i++)
    {
    	cin>>A[i];
    }
    swap(A);
    cout << "Values after Swapping" << m << n << o;
    return 0;
}
